# Dependencies
node_modules
.pnp
.pnp.js

# Build output
.next
out
dist
build

# Environment files (NEVER commit these)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE
.idea
.vscode
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Testing
coverage

# Misc
*.log
*.tsbuildinfo
next-env.d.ts
node_modules_corrupted/

# Large image assets (served via API, not git)
public/Packshot Folder/

# Catalogue PDFs must ship with the app (home-page downloads). Keep them
# tracked even though the folder is large — otherwise production 404s.

# Database backups (contain production data)
backups/
showroom-accestory-identifiants.txt